# 📄 简历-项目经历

## 项目名称

**DeepSeek Harness 视觉路由集成** —— 为纯文本模型 AI 开发环境引入视觉能力的部署工程

| 项 | 内容 |
|---|---|
| **项目时间** | 2026 年 8 月 |
| **我的角色** | 独立负责(需求分析、技术选型、部署实施、验证与文档) |
| **技术栈** | DeepSeek Harness · Cordis 插件框架 · Node.js · pnpm · YAML · PowerShell |
| **项目链接** | [GitHub 仓库地址待填写] |

---

## 一、项目背景

DeepSeek Harness(DSH)是一个 AI 智能体开发环境,其会话模型(deepseek-v4-flash)为纯文本推理模型,**不具备图像输入能力**。实际使用中产生了明确需求:

- 需要"看图"能力:截图分析、图像问答、OCR 文字识别、像素级对比验证等
- 但不能更换会话模型(成本、上下文、推理质量均为关键约束)

**目标**:为 DSH 引入视觉能力,同时保持会话模型不变——"DeepSeek 负责思考,视觉模型负责看"。

## 二、我的职责与主要工作

### 1. 技术选型与方案评估
- 调研并评估社区视觉路由插件 `dsh-vision-router`(GitHub 737★,MIT 协议,持续维护)
- 确认其核心架构满足约束:**按轮次路由**——含图片的轮次自动切换视觉模型,文字轮次保持原模型,成本与上下文零影响
- 核实环境兼容性:Node 版本(^22.19/≥24)、DSH rc.7 宿主、bundle 补丁机制、无 Python 依赖

### 2. 风险前置:故障预案先行
- 复盘历史事故(社区插件曾导致宿主崩溃:加载器报错、配置 JSON 损坏),将其转化为**崩溃处理手册**
- 手册覆盖:7 类崩溃症状速查表、分级恢复流程(杀进程→doctor/repair→手动兜底→重启验证)、彻底卸载路径、6 条预防铁律
- 部署前完成配置备份(`package.json.bak`)

### 3. 部署实施
- 使用官方 CLI 一条命令安装(`dsh plugin --profile web add dsh-vision-router`),自动完成依赖、bundle 注册与 lockfile 同步(106 个依赖包,17.5s)
- 验证与既有皮肤插件共存,不破坏现有配置

### 4. 验证与质量保障
- 逐项验证:配置写入正确性、文件编码无 BOM、bundle 注册完整性、模块可加载性(动态 import 预检,确认 `apply` 导出为函数,杜绝 `invalid plugin` 崩溃)
- 全部通过后交付

## 三、技术难点与解决方案

| 难点 | 解决方案 |
|------|---------|
| 纯文本模型无视觉能力,且不能换模型 | 引入按轮次路由的视觉插件:图片轮走视觉模型,文字轮保持原模型,实现"零替换"接入 |
| 历史上有插件导致宿主崩溃(加载器报错 `invalid plugin, received object`) | 根因分析:安装/运行期间文件被外部写入导致**文件竞态**;产出铁律——"运行期间不改源码、重启前杀进程、装完先重启再配置" |
| 配置损坏导致宿主无法启动(JSON 尾逗号、UTF-8 BOM) | 产出诊断与修复方案:先验证文件首字节与 JSON 合法性,再用插件自带 `doctor`/`repair` 工具自动修复,最后手动兜底 |
| 插件与既有皮肤/宿主的共存 | 通过 bundle 补丁机制(声明式补丁,不手工编辑 cordis 配置)实现多插件共存,验证通过 |

## 四、项目成果

- ✅ 为 DSH 成功集成视觉能力:图片问答、OCR、像素对比、截图分析等 14 个视觉工具可用,免 Key、免 Python
- ✅ 会话模型零更换,文字轮次成本与上下文不受影响
- ✅ 零事故交付:部署、验证全程无崩溃,预检全部通过
- ✅ 沉淀可复用方法论:崩溃处理手册(症状速查 + 恢复流程 + 预防铁律),后续任何插件操作均可遵循
- ✅ 项目完整可复现:README 含架构图、部署步骤、验证记录表

## 五、项目收获

1. **插件化架构理解**:深入 Cordis 插件框架的 bundle 补丁、loader 注册、client/platform 注入机制
2. **工程化思维**:先预案后实施、备份先行、逐项验证——"装上能用"不是完成,可复现、可回滚、可诊断才是
3. **排障能力**:从 JSON 编码(UTF-8 BOM)到文件竞态,积累了真实的"宿主崩溃"排障经验
4. **文档能力**:从用户视角撰写应急手册,把事故经验转化为可执行的标准操作流程(SOP)

---

## 六、面试一句话总结(备用)

> 我独立完成了一个 AI 开发环境(DeepSeek Harness)的视觉能力集成项目:在纯文本会话模型不变的前提下,通过评估社区插件并建立故障预案,安全地为系统引入了 14 个视觉工具,全程零事故,并沉淀了一套包含症状速查与恢复流程的插件管理手册。
